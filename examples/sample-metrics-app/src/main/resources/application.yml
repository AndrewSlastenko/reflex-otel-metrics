spring:
  application:
    name: sample-metrics-app
  profiles:
    # Demo defaults to in-memory H2. Override in deployment, e.g. SPRING_PROFILES_ACTIVE=prod.
    default: local
  config:
    import: optional:classpath:application-reflex.yml
  sql:
    init:
      mode: never

app:
  metrics-lock:
    schema: telemetry
