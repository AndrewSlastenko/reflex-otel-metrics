# AGENTS.md — reflex-otel-metrics

Инструкции для агентов и людей, которые меняют этот репозиторий. Подробные примеры конфигурации и контрактов — в [README.md](README.md).

## Что это за проект

Библиотека **Spring Boot 3** (автоконфигурация JAR): плановый сбор метрик из **JDBC** и публикация в **OpenTelemetry** (OTLP/gRPC), плюс **ручные** метрики из кода приложения через фабрику и типизированные API-интерфейсы. Есть ограничение кардинальности серий, политики overflow, интеграция с **ShedLock** для распределённых задач.

## Стек и версии

| Компонент | Значение |
|-----------|----------|
| Java | 17 (`maven.compiler.release`) |
| Сборка | Maven; на Windows — `.\mvnw.cmd` |
| Spring Boot | 3.5.x (см. `pom.xml`) |
| OpenTelemetry | 1.60.x (BOM в `pom.xml`) |
| Тесты | JUnit 5, `spring-boot-starter-test` |

Точные версии всегда смотреть в `pom.xml`, не копировать из памяти.

## Структура пакетов (`com.reflex.otelmetrics`)

| Пакет | Назначение |
|-------|------------|
| `api` | Публичные контракты: `JdbcMetricSource`, `MetricSource`, `MetricDefinition`, виды метрик, схемы атрибутов, расписание |
| `autoconfigure` | `ReflexOtelMetricsAutoConfiguration`, регистрация бинов OTel и стартера |
| `config` | `ReflexOtelMetricsProperties`, резолверы/валидаторы конфигурации JDBC и manual |
| `jdbc` | Сбор JDBC-метрик (`JdbcMetricCollector` и фабрика) |
| `manual` | `ReflexMetricFactory`, реализации counter/gauge/up-down-counter, валидация атрибутов, трекер серий |
| `otel` | Реестр инструментов, публикация в OTel API |
| `runtime` | Планировщик, задачи выполнения, реестр источников, лимиты серий |
| `locking` | Абстракция блокировки и реализация на ShedLock |
| `internal` | Вспомогательная телеметрия и логирование (не публичный API) |

Точка входа автоконфигурации: `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports`.

## Команды

Из корня репозитория:

```powershell
.\mvnw.cmd test
```

Точечный прогон (пример):

```powershell
.\mvnw.cmd -Dtest=ReflexOtelMetricsAutoConfigurationTest test
```

Перед тем как утверждать, что правки готовы, прогоните релевантные тесты (по крайней мере затронутые модули или полный `test`).

## Конфигурация

Префикс свойств: **`reflex.otel.metrics`**.

- JDBC-источники: `reflex.otel.metrics.sources.<metric-id>`
- Ручные метрики: `reflex.otel.metrics.manual.<metric-id>`

Детали ключей, приоритет резолва (defaults бина → YAML) и семантика `GAUGE` vs `UP_DOWN_COUNTER` — в [README.md](README.md).

## Инварианты для изменений кода

1. **Публичный API** — в основном пакет `api` и типы, которые явно предназначены для приложений-потребителей (`JdbcMetricSource`, фабрика/метрики в `manual`). Внутренности `internal` не рассматривать как стабильный контракт без необходимости.
2. **Обратная совместимость**: новые поля в `@ConfigurationProperties` — с разумными default и документацией в метаданных (при необходимости обновить binding, тесты резолвера/валидатора).
3. **Ручные метрики**: ограничения по overflow (например, отсутствие `AGGREGATE_TO_OTHER` для manual) задокументированы в README — не ломать семантику без обновления документации и тестов.
4. **Артефакты сборки**: не коммитить содержимое `target/`; при локальной проверке оно пересоздаётся Maven.
5. **Стиль правок**: минимальный дифф под задачу; повторять существующие паттерны именования, Lombok/Mapstruct там, где уже используются.

## Документы по дизайну

В `docs/superpowers/` лежат черновики спецификаций и планов (даты в именах файлов). Имеет смысл сверяться при крупных изменениях архитектуры, не дублируя их целиком в коде комментариями.

## Язык

Сообщения коммитов и обсуждения в этом репозитории — по договорённости команды; технические комментарии в коде на английском, если в файле уже так принято.
